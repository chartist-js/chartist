<aside class="side-navigation">
  <nav>
    <ul class="side-nav">
      {{#each (doxTransform apidox)}}
        {{#each modules}}
          <li class="heading">{{name}}</li>
          {{#each members}}
            <li><a class="text-clipping" href="#{{slugify (concat ctx.type ' ' ctx.string)}}">{{ctx.string}}</a></li>
          {{/each}}
        {{/each}}
      {{/each}}
    </ul>
  </nav>
</aside>