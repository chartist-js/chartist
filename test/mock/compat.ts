import Chartist from 'chartist';

export const {
  Bar: BarChart,
  Line: LineChart,
  Pie: PieChart,
  AutoScaleAxis,
  Svg,
  Interpolation,
  getMultiValue
} = Chartist;

export const { Easing: easings } = Svg;
